#include <string>
#include <cstring>
using namespace std;
class Solution {
public:
	int romanToInt(string s) {
		for (auto ii = 0; ii < s.size(); ++ii)
		{
			if (strcmp(s.at(ii), "I"))
			{

			}
			switch (s.at(ii)) {
				case "I":
			}
		}
	}
};
int main()
{
	Solution();
}
